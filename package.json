{
  "name": "knowledge-base-wiki",
  "version": "1.0.0",
  "description": "Static site builder for the knowledge base wiki",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && echo 'Build complete — open _site/index.html'"
  },
  "dependencies": {
    "marked": "^9.1.6"
  },
  "engines": {
    "node": ">=18"
  }
}
